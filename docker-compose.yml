version: '3.9'

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LOAD_MOCK_DATA=true
    command: uvicorn file_monitoring.api:app --host 0.0.0.0 --port 8000 --reload
