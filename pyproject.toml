[project]
name = "etl-file-monitoring"
version = "0.1.0"
description = "File Delivery Monitoring System for S3 arrivals"
authors = [{name = "AutoGenerated"}]
requires-python = ">=3.11"
readme = "README.md"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn>=0.30.0",
  "pydantic>=2.8.0"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2.0",
  "httpx>=0.27.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
