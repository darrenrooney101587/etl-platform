# Package-level .env example for file_processing
# Copy to packages/file_processing/.env and edit values for local development.

# Local S3 shim root (optional). If set, the S3 client will read objects from disk
# at LOCAL_S3_ROOT/<key> instead of calling AWS S3.
LOCAL_S3_ROOT=./local_s3

# Database connection (choose one approach)
# 1) Full connection URL (preferred)
# DATABASE_URL=postgresql://user:password@db-host:5432/dbname

# 2) Individual DB_* variables (alternative)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=etl
DB_USER=etl
DB_PASSWORD=etl

# AWS (if you want real S3 access locally)
# AWS_ACCESS_KEY_ID=your-key
# AWS_SECRET_ACCESS_KEY=your-secret
# AWS_REGION=us-gov-west-1

# SendGrid / Email Configuration
SENDGRID_API_KEY=your-sendgrid-api-key
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=your-sendgrid-api-key
DEFAULT_EMAIL_RECIPIENT=admin@example.com
EMAIL_FROM_ADDRESS=noreply@etl-platform.example.com
EMAIL_FROM_NAME=ETL Platform Data Quality

# PDF Report Configuration
PDF_REPORT_URL=http://localhost:3000/data-quality-report
PDF_REPORT_TIMEOUT_MS=30000
