apiVersion: v1
kind: Service
metadata:
  name: reporting-seeder
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: reporting-seeder
  ports:
    - name: http
      port: 80
      targetPort: 8080
