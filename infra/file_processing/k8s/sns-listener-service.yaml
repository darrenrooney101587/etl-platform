apiVersion: v1
kind: Service
metadata:
  name: sns-listener
  namespace: file-processing
spec:
  type: LoadBalancer
  selector:
    app: file-processing-sns
  ports:
    - name: http
      port: 80
      targetPort: 8080
